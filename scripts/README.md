# Project Scripts

Place repository-level utilities here. Dataset download helpers and other local automation scripts must not write secrets or commit external datasets.

The current dataset helper is `download_dataset.py`.
